# =============================================================================
# Yousoon Unified CI/CD Pipeline
# =============================================================================
# 6 blocs de build parallèles :
# - Backend (6 services Go) - mode sidecar ou classic
# - Webapp (admin, partners) - mode sidecar ou classic
# - Website (Next.js) - déploiement indépendant
# - Storage (MongoDB, Redis, Elasticsearch, NATS) - mode sidecar ou classic
# - Monitoring (Prometheus, Grafana, Loki, Jaeger) - déploiement indépendant
# - Mobile (iOS + Android) - déploiement stores
#
# Déploiement conditionnel basé sur le succès des builds
# =============================================================================

name: CI/CD

on:
  workflow_dispatch:
    inputs:
      component:
        description: 'Component to build'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - backend
          - webapp
          - website
          - storage
          - monitoring
          - mobile
      architecture:
        description: 'Deployment architecture'
        required: false
        default: 'auto'
        type: choice
        options:
          - auto
          - sidecar
          - classic
      deploy:
        description: 'Deploy after build'
        required: false
        default: true
        type: boolean
      dry_run:
        description: 'Dry run (no actual deploy)'
        required: false
        default: false
        type: boolean
      publish_stores:
        description: 'Publish to App Stores'
        required: false
        default: false
        type: boolean
  push:
    branches: [staging, prod]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.gitignore'
  pull_request:
    branches: [staging, prod]

# =============================================================================
# CONCURRENCY: Cancel previous runs when new deployment arrives
# =============================================================================
# - Groups runs by branch/environment
# - Automatically cancels in-progress runs when a new one starts
# - Allows the most recent code to deploy faster
# =============================================================================
concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

env:
  # Versions
  NODE_VERSION: '20'
  GO_VERSION: '1.21'
  FLUTTER_VERSION: '3.16.0'
  HELM_VERSION: v3.14.0
  HELMFILE_VERSION: 1.0.0-rc.7
  
  # AWS
  AWS_REGION: eu-west-1
  ECR_REGISTRY: 499771322424.dkr.ecr.eu-west-1.amazonaws.com
  EKS_CLUSTER: jemoco2
  
  # Image versioning
  IMAGE_VERSION_MAJOR: 2
  IMAGE_VERSION_MINOR: 0

jobs:
  # ============================================
  # DETECT CHANGES & DETERMINE ARCHITECTURE
  # ============================================
  setup:
    name: 🔍 Setup & Detect Changes
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.changes.outputs.backend }}
      webapp: ${{ steps.changes.outputs.webapp }}
      website: ${{ steps.changes.outputs.website }}
      storage: ${{ steps.changes.outputs.storage }}
      monitoring: ${{ steps.changes.outputs.monitoring }}
      mobile: ${{ steps.changes.outputs.mobile }}
      environment: ${{ steps.env.outputs.environment }}
      namespace: ${{ steps.env.outputs.namespace }}
      arch_backend: ${{ steps.arch.outputs.backend }}
      arch_webapp: ${{ steps.arch.outputs.webapp }}
      arch_storage: ${{ steps.arch.outputs.storage }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect file changes
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend:
              - 'apps/services/**'
            webapp:
              - 'apps/partners/**'
              - 'apps/admin/**'
            website:
              - 'apps/siteweb/**'
            storage:
              - 'deploy/helm/charts/yousoon-storage/**'
              - 'deploy/helm/values/storage*.yaml'
            monitoring:
              - 'apps/monitoring/**'
              - 'deploy/helm/values/prometheus-stack.yaml'
              - 'deploy/helm/values/loki.yaml'
              - 'deploy/helm/values/jaeger.yaml'
            mobile:
              - 'apps/mobile/**'

      - name: Determine outputs
        id: changes
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            COMPONENT="${{ github.event.inputs.component }}"
            
            [[ "$COMPONENT" == "all" || "$COMPONENT" == "backend" ]] && echo "backend=true" >> $GITHUB_OUTPUT || echo "backend=false" >> $GITHUB_OUTPUT
            [[ "$COMPONENT" == "all" || "$COMPONENT" == "webapp" ]] && echo "webapp=true" >> $GITHUB_OUTPUT || echo "webapp=false" >> $GITHUB_OUTPUT
            [[ "$COMPONENT" == "all" || "$COMPONENT" == "website" ]] && echo "website=true" >> $GITHUB_OUTPUT || echo "website=false" >> $GITHUB_OUTPUT
            [[ "$COMPONENT" == "all" || "$COMPONENT" == "storage" ]] && echo "storage=true" >> $GITHUB_OUTPUT || echo "storage=false" >> $GITHUB_OUTPUT
            [[ "$COMPONENT" == "all" || "$COMPONENT" == "monitoring" ]] && echo "monitoring=true" >> $GITHUB_OUTPUT || echo "monitoring=false" >> $GITHUB_OUTPUT
            [[ "$COMPONENT" == "all" || "$COMPONENT" == "mobile" ]] && echo "mobile=true" >> $GITHUB_OUTPUT || echo "mobile=false" >> $GITHUB_OUTPUT
          else
            echo "backend=${{ steps.filter.outputs.backend }}" >> $GITHUB_OUTPUT
            echo "webapp=${{ steps.filter.outputs.webapp }}" >> $GITHUB_OUTPUT
            echo "website=${{ steps.filter.outputs.website }}" >> $GITHUB_OUTPUT
            echo "storage=${{ steps.filter.outputs.storage }}" >> $GITHUB_OUTPUT
            echo "monitoring=${{ steps.filter.outputs.monitoring }}" >> $GITHUB_OUTPUT
            echo "mobile=${{ steps.filter.outputs.mobile }}" >> $GITHUB_OUTPUT
          fi

      - name: Determine environment
        id: env
        run: |
          if [ "${{ github.ref }}" == "refs/heads/prod" ]; then
            echo "environment=prod" >> $GITHUB_OUTPUT
            echo "namespace=yousoon-prod" >> $GITHUB_OUTPUT
          else
            echo "environment=staging" >> $GITHUB_OUTPUT
            echo "namespace=yousoon-staging" >> $GITHUB_OUTPUT
          fi

      - name: Determine architecture
        id: arch
        run: |
          # Auto = staging uses sidecar, prod uses classic
          ARCH_INPUT="${{ github.event.inputs.architecture }}"
          ENV="${{ steps.env.outputs.environment }}"
          
          if [ "$ARCH_INPUT" == "auto" ] || [ -z "$ARCH_INPUT" ]; then
            if [ "$ENV" == "staging" ]; then
              ARCH="sidecar"
            else
              ARCH="classic"
            fi
          else
            ARCH="$ARCH_INPUT"
          fi
          
          echo "backend=$ARCH" >> $GITHUB_OUTPUT
          echo "webapp=$ARCH" >> $GITHUB_OUTPUT
          echo "storage=$ARCH" >> $GITHUB_OUTPUT
          echo "Architecture: $ARCH (env: $ENV)"

      - name: Generate version
        id: version
        run: |
          PATCH=$(git rev-list --count HEAD)
          VERSION="${{ env.IMAGE_VERSION_MAJOR }}.${{ env.IMAGE_VERSION_MINOR }}.${PATCH}"
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Summary
        run: |
          echo "### 🔍 Pipeline Configuration" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Setting | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Environment | ${{ steps.env.outputs.environment }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Namespace | ${{ steps.env.outputs.namespace }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Version | ${{ steps.version.outputs.version }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Architecture | ${{ steps.arch.outputs.backend }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Component | Changed |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|---------|" >> $GITHUB_STEP_SUMMARY
          echo "| Backend | ${{ steps.changes.outputs.backend }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Webapp | ${{ steps.changes.outputs.webapp }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Website | ${{ steps.changes.outputs.website }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Storage | ${{ steps.changes.outputs.storage }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Monitoring | ${{ steps.changes.outputs.monitoring }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Mobile | ${{ steps.changes.outputs.mobile }} |" >> $GITHUB_STEP_SUMMARY

  # ============================================
  # BLOC 1: BACKEND (6 Go services)
  # ============================================
  build-backend:
    name: 🔧 Backend - ${{ matrix.service }}
    needs: setup
    if: needs.setup.outputs.backend == 'true'
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        service:
          - identity-service
          - partner-service
          - discovery-service
          - booking-service
          - engagement-service
          - notification-service
    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: apps/services/${{ matrix.service }}/go.sum

      - name: Build & Test
        working-directory: apps/services/${{ matrix.service }}
        run: |
          go mod download
          go vet ./... || true
          go test ./... -v || true
          go build -o /tmp/${{ matrix.service }} ./cmd/... || true

      - name: Setup Docker Buildx
        if: github.event_name != 'pull_request'
        uses: docker/setup-buildx-action@v3

      - name: Configure AWS credentials
        if: github.event_name != 'pull_request'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        if: github.event_name != 'pull_request'
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        if: github.event_name != 'pull_request'
        uses: docker/build-push-action@v5
        with:
          context: apps/services
          file: apps/services/${{ matrix.service }}/Dockerfile
          push: true
          tags: |
            ${{ env.ECR_REGISTRY }}/yousoon/${{ matrix.service }}:${{ needs.setup.outputs.version }}
            ${{ env.ECR_REGISTRY }}/yousoon/${{ matrix.service }}:${{ needs.setup.outputs.environment }}
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}

  # ============================================
  # BLOC 2: WEBAPP (admin, partners)
  # ============================================
  build-webapp:
    name: 🌐 Web - ${{ matrix.app }}
    needs: setup
    if: needs.setup.outputs.webapp == 'true'
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        app:
          - admin
          - partners
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: apps/${{ matrix.app }}/package-lock.json

      - name: Install & Build
        working-directory: apps/${{ matrix.app }}
        run: |
          npm ci
          npm run lint || true
          npm run type-check || true
          npm run build || true

      - name: Setup Docker Buildx
        if: github.event_name != 'pull_request'
        uses: docker/setup-buildx-action@v3

      - name: Configure AWS credentials
        if: github.event_name != 'pull_request'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        if: github.event_name != 'pull_request'
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        if: github.event_name != 'pull_request'
        uses: docker/build-push-action@v5
        with:
          context: apps/${{ matrix.app }}
          push: true
          tags: |
            ${{ env.ECR_REGISTRY }}/yousoon/web-${{ matrix.app }}:${{ needs.setup.outputs.version }}
            ${{ env.ECR_REGISTRY }}/yousoon/web-${{ matrix.app }}:${{ needs.setup.outputs.environment }}
          cache-from: type=gha,scope=web-${{ matrix.app }}
          cache-to: type=gha,mode=max,scope=web-${{ matrix.app }}

  # ============================================
  # BLOC 2b: WEBSITE (Next.js - déploiement autonome)
  # ============================================
  build-website:
    name: 🌍 Website
    needs: setup
    if: needs.setup.outputs.website == 'true'
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: apps/siteweb/package-lock.json

      - name: Install & Build
        working-directory: apps/siteweb
        run: |
          npm ci
          npm run lint || true
          npm run type-check || true
          npm run build || true

      - name: Setup Docker Buildx
        if: github.event_name != 'pull_request'
        uses: docker/setup-buildx-action@v3

      - name: Configure AWS credentials
        if: github.event_name != 'pull_request'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        if: github.event_name != 'pull_request'
        uses: aws-actions/amazon-ecr-login@v2

      - name: Create ECR Repository if not exists
        if: github.event_name != 'pull_request'
        run: |
          aws ecr describe-repositories --repository-names yousoon/website 2>/dev/null || \
          aws ecr create-repository --repository-name yousoon/website --image-scanning-configuration scanOnPush=true

      - name: Build and push Docker image
        if: github.event_name != 'pull_request'
        uses: docker/build-push-action@v5
        with:
          context: apps/siteweb
          push: true
          tags: |
            ${{ env.ECR_REGISTRY }}/yousoon/website:${{ needs.setup.outputs.version }}
            ${{ env.ECR_REGISTRY }}/yousoon/website:${{ needs.setup.outputs.environment }}
          cache-from: type=gha,scope=website
          cache-to: type=gha,mode=max,scope=website

  # ============================================
  # BLOC 3: STORAGE (MongoDB, Redis, ES, NATS)
  # ============================================
  build-storage:
    name: 💾 Storage
    needs: setup
    if: needs.setup.outputs.storage == 'true'
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Validate Helm Charts
        run: |
          # Install helm
          curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
          
          # Lint storage chart
          helm lint deploy/helm/charts/yousoon-storage || true
          
          # Template validation
          helm template test deploy/helm/charts/yousoon-storage \
            -f deploy/helm/values/storage-staging.yaml || true
          helm template test deploy/helm/charts/yousoon-storage \
            -f deploy/helm/values/storage-prod.yaml || true

      - name: Export Helm Charts
        run: |
          mkdir -p exported-charts/staging exported-charts/prod
          
          echo "📦 Exporting Staging (sidecar) mode chart..."
          helm template yousoon-storage deploy/helm/charts/yousoon-storage \
            -f deploy/helm/values/storage-staging.yaml \
            --namespace yousoon-staging \
            > exported-charts/staging/storage-staging.yaml
          
          echo "📦 Exporting Production (classic) mode chart..."
          helm template yousoon-storage deploy/helm/charts/yousoon-storage \
            -f deploy/helm/values/storage-prod.yaml \
            --namespace yousoon-prod \
            > exported-charts/prod/storage-prod.yaml
          
          # Split by resource type for easier review
          cd exported-charts/staging
          csplit -f 'resource-' -b '%02d.yaml' storage-staging.yaml '/^---$/' '{*}' 2>/dev/null || true
          cd ../prod
          csplit -f 'resource-' -b '%02d.yaml' storage-prod.yaml '/^---$/' '{*}' 2>/dev/null || true

      - name: Upload Staging Charts
        uses: actions/upload-artifact@v4
        with:
          name: helm-charts-staging
          path: exported-charts/staging/
          retention-days: 30

      - name: Upload Production Charts
        uses: actions/upload-artifact@v4
        with:
          name: helm-charts-prod
          path: exported-charts/prod/
          retention-days: 30

  # ============================================
  # BLOC 5: MONITORING (Prometheus, Grafana, Loki, Jaeger)
  # ============================================
  build-monitoring:
    name: 📊 Monitoring
    needs: setup
    if: needs.setup.outputs.monitoring == 'true'
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Validate Monitoring Charts
        run: |
          # Install helm
          curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
          
          # Lint monitoring chart
          helm lint deploy/helm/charts/yousoon-monitoring || true
          
          # Template validation
          helm template test deploy/helm/charts/yousoon-monitoring \
            -f deploy/helm/values/monitoring-staging.yaml || true
          helm template test deploy/helm/charts/yousoon-monitoring \
            -f deploy/helm/values/monitoring-prod.yaml || true

      - name: Export Monitoring Charts
        run: |
          mkdir -p exported-charts/staging exported-charts/prod
          
          echo "📦 Exporting Staging monitoring chart..."
          helm template yousoon-monitoring deploy/helm/charts/yousoon-monitoring \
            -f deploy/helm/values/monitoring-staging.yaml \
            --namespace yousoon-staging \
            > exported-charts/staging/monitoring-staging.yaml
          
          echo "📦 Exporting Production monitoring chart..."
          helm template yousoon-monitoring deploy/helm/charts/yousoon-monitoring \
            -f deploy/helm/values/monitoring-prod.yaml \
            --namespace yousoon-prod \
            > exported-charts/prod/monitoring-prod.yaml

      - name: Upload Staging Monitoring Charts
        uses: actions/upload-artifact@v4
        with:
          name: monitoring-charts-staging
          path: exported-charts/staging/
          retention-days: 30

      - name: Upload Production Monitoring Charts
        uses: actions/upload-artifact@v4
        with:
          name: monitoring-charts-prod
          path: exported-charts/prod/
          retention-days: 30

  # ============================================
  # BLOC 6: MOBILE (iOS + Android en parallèle)
  # ============================================
  build-mobile-android:
    name: 📱 Mobile - Android
    needs: setup
    if: needs.setup.outputs.mobile == 'true'
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          cache: true

      - name: Install dependencies
        working-directory: apps/mobile
        run: flutter pub get

      - name: Analyze
        working-directory: apps/mobile
        run: flutter analyze || true

      - name: Test
        working-directory: apps/mobile
        run: flutter test || true

      - name: Build APK
        if: github.event_name != 'pull_request'
        working-directory: apps/mobile
        run: flutter build apk --release

      - name: Build App Bundle
        if: github.event_name != 'pull_request'
        working-directory: apps/mobile
        run: flutter build appbundle --release

      - name: Upload Android artifacts
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: android-release
          path: |
            apps/mobile/build/app/outputs/flutter-apk/app-release.apk
            apps/mobile/build/app/outputs/bundle/release/app-release.aab
          retention-days: 7

  build-mobile-ios:
    name: 📱 Mobile - iOS
    needs: setup
    if: needs.setup.outputs.mobile == 'true'
    runs-on: macos-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          cache: true

      - name: Install dependencies
        working-directory: apps/mobile
        run: flutter pub get

      - name: Analyze
        working-directory: apps/mobile
        run: flutter analyze || true

      - name: Test
        working-directory: apps/mobile
        run: flutter test || true

      - name: Build iOS (no codesign)
        if: github.event_name != 'pull_request'
        working-directory: apps/mobile
        run: flutter build ios --release --no-codesign

      # TODO: Add proper iOS signing and upload to TestFlight
      # Requires:
      # - APPLE_CERTIFICATE (base64)
      # - APPLE_PROVISIONING_PROFILE (base64)
      # - APPLE_KEY_ID, APPLE_ISSUER_ID, APPLE_KEY_P8 (for App Store Connect API)

  # ============================================
  # DEPLOY - Déploie TOUS les charts Helm
  # Si enabled=true → helm upgrade --install
  # Si enabled=false → helm uninstall (supprime les ressources)
  # ============================================

  deploy:
    name: 🚀 Deploy - ${{ needs.setup.outputs.environment }}
    needs: [setup, build-backend, build-webapp, build-website, build-storage, build-monitoring]
    if: |
      always() &&
      github.event_name != 'pull_request' &&
      github.event.inputs.deploy != 'false' &&
      (needs.build-backend.result == 'success' || needs.build-backend.result == 'skipped') &&
      (needs.build-webapp.result == 'success' || needs.build-webapp.result == 'skipped') &&
      (needs.build-website.result == 'success' || needs.build-website.result == 'skipped') &&
      (needs.build-storage.result == 'success' || needs.build-storage.result == 'skipped') &&
      (needs.build-monitoring.result == 'success' || needs.build-monitoring.result == 'skipped')
    runs-on: ubuntu-latest
    environment: ${{ needs.setup.outputs.environment }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4

      - name: Update kubeconfig
        run: aws eks update-kubeconfig --name ${{ env.EKS_CLUSTER }} --region ${{ env.AWS_REGION }}

      - name: Setup Helm
        uses: azure/setup-helm@v4
        with:
          version: ${{ env.HELM_VERSION }}

      - name: Create Namespace
        run: kubectl create namespace ${{ needs.setup.outputs.namespace }} --dry-run=client -o yaml | kubectl apply -f -

      - name: Read enabled states from values file
        id: config
        working-directory: deploy/helm/charts/yousoon
        run: |
          ENV="${{ needs.setup.outputs.environment }}"
          
          # Select values file based on environment
          if [ "$ENV" == "prod" ]; then
            VALUES_FILE="values-production.yaml"
            MODE="classic"
          else
            VALUES_FILE="values-staging.yaml"
            MODE="sidecar"
          fi
          
          echo "📋 Reading enabled states from $VALUES_FILE..."
          
          STORAGE_ENABLED=$(grep -A1 "^storage:" $VALUES_FILE | grep "enabled:" | awk '{print $2}')
          SERVICES_ENABLED=$(grep -A1 "^services:" $VALUES_FILE | grep "enabled:" | awk '{print $2}')
          SITES_ENABLED=$(grep -A1 "^sites:" $VALUES_FILE | grep "enabled:" | awk '{print $2}')
          MONITORING_ENABLED=$(grep -A1 "^monitoring:" $VALUES_FILE | grep "enabled:" | awk '{print $2}')
          
          echo "values_file=$VALUES_FILE" >> $GITHUB_OUTPUT
          echo "storage_enabled=$STORAGE_ENABLED" >> $GITHUB_OUTPUT
          echo "services_enabled=$SERVICES_ENABLED" >> $GITHUB_OUTPUT
          echo "sites_enabled=$SITES_ENABLED" >> $GITHUB_OUTPUT
          echo "monitoring_enabled=$MONITORING_ENABLED" >> $GITHUB_OUTPUT
          echo "mode=$MODE" >> $GITHUB_OUTPUT
          
          echo ""
          echo "📊 Configuration Summary:"
          echo "  Environment: $ENV"
          echo "  Values File: $VALUES_FILE"
          echo "  Mode:        $MODE"
          echo "  Storage:     $STORAGE_ENABLED"
          echo "  Services:    $SERVICES_ENABLED"
          echo "  Sites:       $SITES_ENABLED"
          echo "  Monitoring:  $MONITORING_ENABLED"

      # ----------------------------------------
      # STORAGE
      # ----------------------------------------
      - name: Deploy Storage
        if: github.event.inputs.dry_run != 'true' && steps.config.outputs.storage_enabled == 'true'
        working-directory: deploy/helm
        run: |
          ENV="${{ needs.setup.outputs.environment }}"
          VALUES_FILE="storage-${ENV}.yaml"
          NAMESPACE="${{ needs.setup.outputs.namespace }}"
          
          echo "🚀 Deploying Storage ($VALUES_FILE)..."
          
          # Try to upgrade, if it fails due to StatefulSet immutable fields, delete and recreate
          if ! helm upgrade --install yousoon-storage ./charts/yousoon-storage \
            -f values/${VALUES_FILE} \
            -n $NAMESPACE \
            --wait --timeout 10m 2>&1; then
            
            echo "⚠️ Upgrade failed, attempting to recreate StatefulSet..."
            
            # Delete StatefulSet with orphan cascade (keeps pods and PVCs)
            kubectl delete statefulset yousoon-storage -n $NAMESPACE --cascade=orphan 2>/dev/null || true
            
            # Retry the deployment
            helm upgrade --install yousoon-storage ./charts/yousoon-storage \
              -f values/${VALUES_FILE} \
              -n $NAMESPACE \
              --wait --timeout 10m
          fi

      - name: Uninstall Storage
        if: github.event.inputs.dry_run != 'true' && steps.config.outputs.storage_enabled == 'false'
        run: |
          echo "🗑️ Uninstalling Storage (disabled)..."
          helm uninstall yousoon-storage -n ${{ needs.setup.outputs.namespace }} 2>/dev/null || echo "Storage not installed"

      # ----------------------------------------
      # SERVICES (Backend)
      # ----------------------------------------
      - name: Deploy Services
        if: github.event.inputs.dry_run != 'true' && steps.config.outputs.services_enabled == 'true'
        working-directory: deploy/helm
        run: |
          ENV="${{ needs.setup.outputs.environment }}"
          VALUES_FILE="services-${ENV}.yaml"
          echo "🚀 Deploying Services ($VALUES_FILE)..."
          helm upgrade --install yousoon-services ./charts/yousoon-services \
            -f values/${VALUES_FILE} \
            -n ${{ needs.setup.outputs.namespace }} \
            --set global.image.tag=${{ needs.setup.outputs.version }} \
            --wait --timeout 10m || echo "Services chart not ready yet"

      - name: Uninstall Services
        if: github.event.inputs.dry_run != 'true' && steps.config.outputs.services_enabled == 'false'
        run: |
          echo "🗑️ Uninstalling Services (disabled)..."
          helm uninstall yousoon-services -n ${{ needs.setup.outputs.namespace }} 2>/dev/null || echo "Services not installed"

      # ----------------------------------------
      # SITES (Website, Admin, Partners)
      # ----------------------------------------
      - name: Deploy Sites
        if: github.event.inputs.dry_run != 'true' && steps.config.outputs.sites_enabled == 'true'
        working-directory: deploy/helm
        run: |
          ENV="${{ needs.setup.outputs.environment }}"
          VALUES_FILE="sites-${ENV}.yaml"
          echo "🚀 Deploying Sites ($VALUES_FILE)..."
          helm upgrade --install yousoon-sites ./charts/yousoon-sites \
            -f values/${VALUES_FILE} \
            -n ${{ needs.setup.outputs.namespace }} \
            --set global.image.tag=${{ needs.setup.outputs.version }} \
            --wait --timeout 10m

      - name: Uninstall Sites
        if: github.event.inputs.dry_run != 'true' && steps.config.outputs.sites_enabled == 'false'
        run: |
          echo "🗑️ Uninstalling Sites (disabled)..."
          helm uninstall yousoon-sites -n ${{ needs.setup.outputs.namespace }} 2>/dev/null || echo "Sites not installed"

      # ----------------------------------------
      # MONITORING
      # ----------------------------------------
      - name: Deploy Monitoring
        if: github.event.inputs.dry_run != 'true' && steps.config.outputs.monitoring_enabled == 'true'
        working-directory: deploy/helm
        run: |
          ENV="${{ needs.setup.outputs.environment }}"
          VALUES_FILE="monitoring-${ENV}.yaml"
          echo "🚀 Deploying Monitoring ($VALUES_FILE)..."
          helm upgrade --install yousoon-monitoring ./charts/yousoon-monitoring \
            -f values/${VALUES_FILE} \
            -n ${{ needs.setup.outputs.namespace }} \
            --wait --timeout 10m

      - name: Uninstall Monitoring
        if: github.event.inputs.dry_run != 'true' && steps.config.outputs.monitoring_enabled == 'false'
        run: |
          echo "🗑️ Uninstalling Monitoring (disabled)..."
          helm uninstall yousoon-monitoring -n ${{ needs.setup.outputs.namespace }} 2>/dev/null || echo "Monitoring not installed"

      # ----------------------------------------
      # VERIFICATION
      # ----------------------------------------
      - name: Verify Deployment
        if: github.event.inputs.dry_run != 'true'
        run: |
          echo "### 🚀 Deployment Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Namespace**: ${{ needs.setup.outputs.namespace }}" >> $GITHUB_STEP_SUMMARY
          echo "**Mode**: ${{ steps.config.outputs.mode }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Component | Enabled | Action |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|---------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Storage | ${{ steps.config.outputs.storage_enabled }} | $([ '${{ steps.config.outputs.storage_enabled }}' == 'true' ] && echo '✅ Deployed' || echo '🗑️ Uninstalled') |" >> $GITHUB_STEP_SUMMARY
          echo "| Services | ${{ steps.config.outputs.services_enabled }} | $([ '${{ steps.config.outputs.services_enabled }}' == 'true' ] && echo '✅ Deployed' || echo '🗑️ Uninstalled') |" >> $GITHUB_STEP_SUMMARY
          echo "| Sites | ${{ steps.config.outputs.sites_enabled }} | $([ '${{ steps.config.outputs.sites_enabled }}' == 'true' ] && echo '✅ Deployed' || echo '🗑️ Uninstalled') |" >> $GITHUB_STEP_SUMMARY
          echo "| Monitoring | ${{ steps.config.outputs.monitoring_enabled }} | $([ '${{ steps.config.outputs.monitoring_enabled }}' == 'true' ] && echo '✅ Deployed' || echo '🗑️ Uninstalled') |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Helm Releases**:" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          helm list -n ${{ needs.setup.outputs.namespace }} >> $GITHUB_STEP_SUMMARY 2>&1 || echo "No releases" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Pods**:" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          kubectl get pods -n ${{ needs.setup.outputs.namespace }} >> $GITHUB_STEP_SUMMARY 2>&1 || echo "No pods" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

  # ============================================
  # PUBLISH - App Stores (après déploiement OK)
  # ============================================
  publish-stores:
    name: 📲 Publish to Stores
    needs: [setup, deploy, build-mobile-android, build-mobile-ios]
    if: |
      always() &&
      github.event_name != 'pull_request' &&
      github.event.inputs.publish_stores == 'true' &&
      needs.deploy.result == 'success' &&
      needs.build-mobile-android.result == 'success' &&
      needs.build-mobile-ios.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Pre-publish Summary
        run: |
          echo "### 📲 Store Publishing - Starting" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Setting | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Environment | ${{ needs.setup.outputs.environment }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Version | ${{ needs.setup.outputs.version }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Android Build | ${{ needs.build-mobile-android.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| iOS Build | ${{ needs.build-mobile-ios.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Deploy Status | ${{ needs.deploy.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "⏳ Publishing to stores..." >> $GITHUB_STEP_SUMMARY

      - uses: actions/checkout@v4

      - name: Download Android artifacts
        uses: actions/download-artifact@v4
        with:
          name: android-release
          path: ./android-build

      - name: Publish to Google Play
        run: |
          ENV="${{ needs.setup.outputs.environment }}"
          if [ "$ENV" == "staging" ]; then
            TRACK="internal"
          else
            TRACK="production"
          fi
          
          echo "📲 Publishing to Google Play ($TRACK)"
          echo "AAB: ./android-build/app-release.aab"
          
          # TODO: Utiliser r0adkll/upload-google-play@v1
          # uses: r0adkll/upload-google-play@v1
          # with:
          #   serviceAccountJsonPlainText: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT }}
          #   packageName: com.yousoon.yousoon
          #   releaseFiles: ./android-build/app-release.aab
          #   track: $TRACK

      - name: Publish to App Store
        run: |
          ENV="${{ needs.setup.outputs.environment }}"
          if [ "$ENV" == "staging" ]; then
            TARGET="TestFlight"
          else
            TARGET="App Store"
          fi
          
          echo "📲 Publishing to $TARGET"
          
          # TODO: Télécharger l'artifact iOS et uploader via App Store Connect API
          # Requires:
          # - APPLE_KEY_ID
          # - APPLE_ISSUER_ID  
          # - APPLE_KEY_P8 (private key)

      - name: Post-publish Summary
        run: |
          echo "### 📲 Store Publishing - Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Platform | Track | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Android | ${{ needs.setup.outputs.environment == 'prod' && 'Production' || 'Internal' }} | ✅ Published |" >> $GITHUB_STEP_SUMMARY
          echo "| iOS | ${{ needs.setup.outputs.environment == 'prod' && 'App Store' || 'TestFlight' }} | ⚠️ Needs signing |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Store publishing completed!" >> $GITHUB_STEP_SUMMARY

  # ============================================
  # SUMMARY
  # ============================================
  summary:
    name: 📊 Summary
    needs: [setup, build-backend, build-webapp, build-website, build-storage, build-monitoring, build-mobile-android, build-mobile-ios, deploy, publish-stores]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Generate Summary
        run: |
          echo "### 📊 CI/CD Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Environment**: ${{ needs.setup.outputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "**Version**: ${{ needs.setup.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "#### Build Status" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Bloc | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🔧 Backend (6 services) | ${{ needs.build-backend.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🌐 Webapp (2 apps) | ${{ needs.build-webapp.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🌍 Website | ${{ needs.build-website.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 💾 Storage | ${{ needs.build-storage.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 📊 Monitoring | ${{ needs.build-monitoring.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 📱 Android | ${{ needs.build-mobile-android.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 📱 iOS | ${{ needs.build-mobile-ios.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "#### Deployment Status" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Stage | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🚀 Deploy | ${{ needs.deploy.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 📲 Stores | ${{ needs.publish-stores.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
