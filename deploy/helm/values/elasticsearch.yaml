# Elasticsearch - Production overrides
# Passer à 3 nodes pour HA plus tard
# replicas: 3
# minimumMasterNodes: 2

resources:
  requests:
    memory: 2Gi
    cpu: 500m
  limits:
    memory: 4Gi
    cpu: 2000m

esJavaOpts: "-Xmx2g -Xms2g"

volumeClaimTemplate:
  storageClassName: gp3
  resources:
    requests:
      storage: 50Gi
