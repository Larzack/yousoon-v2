# =============================================================================
# Loki - Configuration (pour yousoon-monitoring chart)
# =============================================================================
# Ce fichier peut être utilisé comme référence ou override pour Loki
# Le fichier principal est monitoring-staging.yaml ou monitoring-prod.yaml
# =============================================================================

loki:
  enabled: true
  image: grafana/loki:2.9.2
  port: 3100
  
  resources: {}
  
  persistence:
    enabled: true
    size: 100Gi
    storageClass: "gp3"
